package com.birb_birb.blockbounce.api;

/**
 * Marker interface for objects that can collide with others.
 * Keep minimal for now; expand later if needed (e.g., collision handlers).
 */
public interface Collidable {
}

